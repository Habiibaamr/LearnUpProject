# Development (recommended): call FastAPI directly — avoids proxy/cache mismatches
VITE_API_URL=http://127.0.0.1:8000

# Alternative: use Vite proxy (see vite.config.ts) — same-origin /api → :8000
# VITE_API_URL=/api
